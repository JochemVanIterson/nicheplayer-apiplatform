export default function () {
  return {
    isLoggingIn: false,
    jwtToken: '',
    refreshToken: '',
    userData: {},

    /**
     * State if webAudio and it's features should be enabled
     */
    enableWebAudio: true
  }
}
